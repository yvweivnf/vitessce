import type { Codec, CompressorConfig } from 'numcodecs';
interface Options {
    level?: number;
    cname?: string;
    blocksize?: number;
    clevel?: number;
    shuffle?: number;
}
declare type CodecConstructor = {
    fromConfig(config: Options & CompressorConfig): Codec;
};
declare type CodecImporter = () => CodecConstructor | Promise<CodecConstructor>;
export declare function addCodec(id: string, importFn: CodecImporter): void;
export declare function getCodec<T extends Codec>(config: Options & CompressorConfig): Promise<T>;
export {};
