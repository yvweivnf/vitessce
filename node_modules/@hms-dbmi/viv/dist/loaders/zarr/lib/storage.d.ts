import type { AsyncStore } from 'zarr/types/storage/types';
declare class ReadOnlyStore {
    keys(): Promise<never[]>;
    deleteItem(): Promise<boolean>;
    setItem(): Promise<boolean>;
}
export declare class FileStore extends ReadOnlyStore implements AsyncStore<ArrayBuffer> {
    private _map;
    private _rootPrefix;
    constructor(fileMap: Map<string, File>, rootPrefix?: string);
    private _key;
    getItem(key: string): Promise<ArrayBuffer>;
    containsItem(key: string): Promise<boolean>;
}
export {};
